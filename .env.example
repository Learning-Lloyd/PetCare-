# Copy to .env and set values to match phpMyAdmin (same MySQL/MariaDB server).
# In phpMyAdmin: user + database name on the left must match DB_USER / DB_NAME.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=petcareplus

PORT=3001

# After DB_* is correct: npm run migrate
